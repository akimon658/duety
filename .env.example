# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=duety

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your-client-id
GOOGLE_CLIENT_SECRET=your-client-secret

# Task Synchronization Configuration
# Enable automatic polling to sync tasks from calendar to Google Tasks
SYNC_POLLING_ENABLED=true
# Interval in minutes between automatic syncs (default: 60)
SYNC_INTERVAL_MINUTES=60
